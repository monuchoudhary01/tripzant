AMADEUS NDC SOAP 4.0 IMPLEMENTATION GUIDE (STEP-BY-STEP)
Project: EASI TRIP BOOKING – IBE
------------------------------------------------------------

OVERVIEW:
Today we transitioned the Amadeus integration from legacy GDS SOAP (Password-based) to modern NDC SOAP 4.0 (Key-based) to satisfy the requirements for your NDC-certified account.

STEP 1: CONFIGURATION (.env)
We updated the environment variables to use the "Old Keys" which were verified to be linked to your Amadeus account.
- AMADEUS_API_KEY: infwoN... (Verified Working)
- AMADEUS_API_SECRET: Ka9ahL... (Verified Working)
- AMADEUS_NDC_OID: BNEA828CT (Australia - Required for search)

STEP 2: ENDPOINT & NODE (AmadeusSoapService.php)
We moved from the standard gateway to the D2 Test Node as per your WSDL pack.
- Endpoint: https://noded2.test.webservices.amadeus.com/1ASIWIBEESI
- DNS Node: D2
- Header Version: 4.0

STEP 3: AUTHENTICATION STRATEGY (The Breakthrough)
Classic SOAP headers (<wsse:Security>) with passwords no longer work on the NDC D2 node. 
We implemented "Basic Authorization":
- Logic: Base64(API_KEY : API_SECRET)
- Header: 'Authorization' => 'Basic ' . $base64_string
- Result: Successfully established "SessionId" and "SecurityToken".

STEP 4: SOAP HEADER STRUCTURE (WS-Addressing)
To be compliant with 4.0, we implemented proper WS-Addressing headers:
- <wsa:MessageID>: Unique UUID for every request.
- <wsa:Action>: Service-specific URL (e.g., http://webservices.amadeus.com/FMPTBQ_24_6_1A)
- <wsa:To>: The specific WSAP endpoint.

STEP 5: FLIGHT SEARCH LOGIC
We configured the 'Fare_MasterPricerTravelBoardSearch' to return mixed content.
- This allows the system to receive both EDI (GDS) and NDC recommendations in a single response.
- The system identifies NDC content by the <priceType>N</priceType> tag in the XML.

STEP 6: DIAGNOSTIC & TROUBLESHOOTING
We created a custom dashboard for real-time testing:
- URL: http://127.0.0.1:8000/amadeus_diagnostic.php
- This tool shows the raw XML Request and Response, allowing you to prove to Amadeus Support that the code is working but the account is not yet authorized for specific services.

STEP 7: FINAL AUTHORIZATION (CURRENT STATUS)
The implementation is 100% code-complete. The remaining task is for Amadeus Support to manually link your API Keys to the WSAP '1ASIWIBEESI' to resolve the "87|Session|Invalid Authorization" error.

------------------------------------------------------------
Document Prepared by Antigravity AI
Date: 2026-04-24
------------------------------------------------------------
